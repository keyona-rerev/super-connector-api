# SUPER CONNECTOR SYSTEM — HANDOFF DOCUMENT
## Session: April 1, 2026
**Owner:** Keyona Meeks
**Status:** Events feature fully built — Railway schema, API endpoints, and web app UI all deployed
**Next session:** Contacts CRM rebuild — browse-first layout with inline search, source/tags field, add/edit/bucket UI

---

## WHAT CHANGED THIS SESSION

### 1. Events Feature — Complete

**Option C hybrid model confirmed:**
- Interview series episodes remain as **sub-projects** under their initiative
- All other events (panels, workshops, networking) are a new **Events** first-class object

#### Railway — `db.py`
Two new tables added to `init_db()` via `CREATE TABLE IF NOT EXISTS` (auto-creates on next deploy, no migration needed):

**`events` table:**
```
event_id     TEXT PRIMARY KEY  (auto: EVT-{timestamp})
data         JSONB NOT NULL
created_at   TIMESTAMP
updated_at   TIMESTAMP
```
Indexes on `data->>'venture'` and `data->>'event_type'`.

JSONB data fields: `event_id` · `event_name` · `event_type` · `venture` · `date` · `location` · `status` · `initiative_id` · `description` · `notes`

**`event_guests` table:**
```
guest_id    TEXT PRIMARY KEY  (auto: EG-{timestamp})
event_id    TEXT NOT NULL
contact_id  TEXT
data        JSONB NOT NULL
created_at  TIMESTAMP
updated_at  TIMESTAMP
```
Indexes on `event_id` and `contact_id`.

JSONB data fields: `guest_id` · `event_id` · `contact_id` · `full_name` · `role` · `guest_status` · `notes`

New db functions: `upsert_event`, `get_event`, `get_all_events` (filterable by `event_type` and `venture`), `delete_event`, `upsert_event_guest`, `get_guests_for_event`, `delete_event_guest`

#### Railway — `models.py`
Four new Pydantic models: `EventPayload`, `EventStatusUpdate`, `EventGuestPayload`, `EventGuestUpdate`

**Status values:** `Planning` / `Confirmed` / `Complete` / `Cancelled`
**Event type values:** `Hosting` / `Attending` / `Workshop`
**Guest role values:** `Speaker` / `Panelist` / `Attendee` / `Interviewee` / `Organizer` / `Sponsor`
**Guest status values:** `Invited` / `Confirmed` / `Attended` / `No-show` / `Declined`

#### Railway — `main.py` — 10 new endpoints
```
GET    /events                    List all events (?type=Hosting&venture=BTC)
GET    /event/{id}                Fetch event + guests + guest_summary {total, confirmed, attended}
POST   /event                     Create event
PUT    /event/{id}                Update event (merge — omitted fields preserved)
DELETE /event/{id}                Remove event
PATCH  /event/{id}/status         Quick status update
GET    /event/{id}/guests         List guests for event
POST   /event-guest               Add guest to event
PATCH  /event-guest/{id}          Update guest role/status/notes (all optional, PATCH semantics)
DELETE /event-guest/{id}          Remove guest from event
```

`GET /event/{id}` returns event data + full guest list + `guest_summary: { total, confirmed, attended }` in a single call.

All 10 endpoints confirmed live in Railway Swagger docs at `/docs`.

#### Web App — `index.html` (keyona-rerev/super-connector-app)
File grew from 81,982 → 98,309 bytes.

**New sidebar nav item:** `◈ Events` (below Content Registry)

**New Events page** (`page-events`):
- 3-tab switcher: Hosting · Attending · Workshops
- Active tab pre-selected when opening Create Event modal
- Event cards with left-border color coding, guest count badge (`confirmed/total`)

**New Event Detail Modal** (`.emodal-overlay`):
- Header: name, status, venture, date, location, connected initiative
- Guest Pipeline section with inline status dropdowns + add-guest form
- Quick actions: Mark Confirmed / Mark Complete

**New JS functions:**
`loadEvents()` · `renderEvents()` · `switchEventsTab()` · `openEventDetail()` · `addGuest()` · `updateGuestStatus()` · `quickStatusUpdate()` · `closeEModal()` · `handleEModalOverlayClick()` · `openEventModal()` · `closeEventModal()` · `saveEvent()`

---

## PREVIOUS SESSION CHANGES (carried forward for context)

### Google Sheets Deleted
Both sheets deleted. Railway is sole source of truth.

### Phoebe GAS — Fully Rewired to Railway
**Script ID:** `1UeO72LgmCgEhr534Aw2ouj7lHKFiXe0mbGSCaFjsf1bphf510SOnkV4e`

### Network CRM GAS — Fully Rewired to Railway
**Script ID:** `144ZiIEVpxYrRB93wq4MoYgtUxqVDQdQ7ZdmHKQH3RGWu-thi8F-6v7hz`

---

## SYSTEM ARCHITECTURE (CURRENT STATE)

```
Gmail / Google Calendar
        ↕
GAS Scripts (two projects — time-triggered)
  • Phoebe: check-ins, reply parsing, decay scan, task sync
  • Network CRM: calendar briefings, email reply processing, Google Contacts import
        ↕
Railway API (FastAPI + pgvector) ← SOLE SOURCE OF TRUTH
  https://super-connector-api-production.up.railway.app
        ↕
GitHub Pages Web App ← PRIMARY UI
  https://keyona-rerev.github.io/super-connector-app
```

---

## RAILWAY DATABASE STATE

| Table | Records | Notes |
|---|---|---|
| `contacts` | 4,401 | Vectorized (512-dim Voyage AI), health scored |
| `initiatives` | 26 | All migrated |
| `sub_projects` | 0 | Empty, ready |
| `stakeholders` | 9 | Sparse |
| `activation_angles` | 0 | Empty, ready |
| `action_items` | 0 | Empty, ready |
| `follow_ups` | 0 | Empty, ready |
| `events` | 0 | ✅ Built this session |
| `event_guests` | 0 | ✅ Built this session |

---

## KEY IDs & CREDENTIALS

| Resource | ID / URL |
|---|---|
| Railway API | `https://super-connector-api-production.up.railway.app` |
| Web App | `https://keyona-rerev.github.io/super-connector-app` |
| GitHub Org | `keyona-rerev` |
| API Repo | `keyona-rerev/super-connector-api` |
| App Repo | `keyona-rerev/super-connector-app` |
| Phoebe GAS Script | `1UeO72LgmCgEhr534Aw2ouj7lHKFiXe0mbGSCaFjsf1bphf510SOnkV4e` |
| Network CRM GAS Script | `144ZiIEVpxYrRB93wq4MoYgtUxqVDQdQ7ZdmHKQH3RGWu-thi8F-6v7hz` |
| Tool Registry Sheet | `1ol8Yvpe454T4yHpEgy8PFjOAJvfyUDrKesgD_wM7m4o` |
| Prismm Martha ABM Sheet | `1FN4NVRb7VfbUg7EO6G01cYm3p9-Q2jS1R9gNl_BU410` |
| Transcript Folder | `1kU30t4XelF4f3HDi7h0dbrq5YIPf15tg` |

**SC_API_KEY:** Ask Keyona at session start. Never hardcode.

---

## OPEN KNOWN ISSUES (CARRIED FORWARD)

1. **Stakeholders table sparse (9 records).** Calendar briefings only fire for those 9.
2. **Interactions not in Railway schema.** `createInteraction` / `getInteractions` in Network CRM GAS are stubbed.
3. **Content tab → Railway sync not built.** `POST /content` endpoint exists. GAS trigger not yet wired.
4. **Follow-up health scoring not built.** Phase 2.
5. **Apollo enrichment pending.** 406 credits remaining. Needs `APOLLO_API_KEY` in Network CRM GAS Script Properties, then run `enrichWithApollo()`. Re-vectorize after.
6. **Google Contacts import.** `importGoogleContacts()` in GoogleSync.gs ready. Trigger installs Sunday 9am via `installGoogleSyncTriggers()`.

---

## NEXT SESSION: CONTACTS CRM REBUILD

### Decisions already made — do not re-ask
- **Layout: Option B** — Contacts page defaults to browsable grid. Search box filters inline on typed text; hitting Enter on a substantive query fires semantic search and shows vector results. Single unified page, no tab switcher needed.
- **Source/tags field: YES** — Add `source` field to `ContactPayload` in `main.py` and `models.py`. Used to tag where a contact was met (e.g. "SXSW 2026", "BTC Summit", "Cold Outreach", "Referral"). Filterable in the browse grid. No schema migration needed (JSONB). This is the first thing to do in the next session.

### Why this matters
The current Search Contacts page is semantic-search-only. There is no way to browse, add, edit, or bucket contacts. SXSW connects and other new warm contacts have not been entered because there is no UI for it. This is the primary blocker to the CRM being useful day-to-day.

### Build Order for Next Session

**Step 1 — Add `source` field to contact schema (2 min)**
- `main.py`: add `source: Optional[str] = ""` to `ContactPayload` class
- `models.py`: no ContactPayload there (it lives in main.py), but verify nothing else needs updating
- `_build_profile_text()` in `main.py`: optionally include source in embedding text (e.g. `f"Met at: {contact.source}"`) so "SXSW" is semantically searchable
- Deploy to Railway

**Step 2 — Browse grid (paginated)**
- Default view: paginated contact grid, 50 per page, sorted by `updated_at DESC`
- Uses existing `GET /contacts?limit=50&offset=N`
- Each card shows: name, role/org, venture tag, relationship_health badge, activation_potential badge, source tag (if set)
- Filter bar: venture dropdown, relationship_health dropdown, activation_potential dropdown, source text input (or dropdown if feasible)
- Pagination controls: Previous / Next, page indicator

**Step 3 — Inline search behavior**
- Search box above the grid
- Short queries (< 4 words, no Enter): filter the already-loaded contacts client-side by name/org
- Enter key or longer query: fire `POST /search` semantic search, replace grid with vector results
- Clear button resets back to browse mode

**Step 4 — Add New Contact modal**
- `+ New Contact` topbar button (same pattern as all other pages)
- Modal fields: full_name*, title_role, organization, how_we_met, source (new), venture, what_building, what_need, what_offer, relationship_health, activation_potential, notes
- `contact_id` auto-generated: `C{timestamp}`
- On save: `POST /contact` — vectorizes server-side automatically

**Step 5 — Edit contact from drawer**
- Existing contact drawer is read-only. Add an `Edit` button to the drawer header.
- Clicking Edit opens the same Add/Edit modal pre-filled with the contact's current data
- On save: `PUT /contact/{id}` — re-vectorizes automatically
- Drawer refreshes after save

**Step 6 — Source bucket quick-add (nice to have)**
- When viewing contacts filtered by source="", offer a quick "Tag source" chip on each card
- Or just leave it to the edit modal — decide at session time based on how much runway is left

### Existing infrastructure to reuse
- `GET /contacts?limit=50&offset=0` — already built
- `POST /contact` — already built, vectorizes
- `PUT /contact/{id}` — already built, re-vectorizes
- `POST /search` — already built, semantic vector search
- Contact drawer HTML + JS — already exists, extend not rebuild
- Contact card CSS (`.contact-card`) — already exists in search results, reuse directly

### What does NOT change
- Railway API: no new endpoints needed for core CRM (existing CRUD covers it)
- T018 Post-Meeting Engine: still writes contacts the same way
- Phoebe: unaffected
- Semantic search: preserved as a mode within the page, not removed

---

## PARKED FOR LATER: INITIATIVE MODAL — EPISODE DETAIL

When a sub-project is clicked in the initiative modal, the detail panel should eventually show episode guest slots and a production status pipeline (Pitching → Scheduled → Recorded → Editing → Published). Decision needed: extend `sub_projects` JSONB or model via event-guests. Not urgent.

---

## VENTURES REFERENCE

| Venture | Focus |
|---|---|
| **ReRev Labs** | AI education, consulting, automation sales |
| **Prismm** | White-label digital vault, B2B to community banks/credit unions. Co-founder: Martha |
| **Black Tech Capital (BTC)** | Climate tech nano VC. Key initiative: Climate Tech Exit Lab interview series |
| **Sekhmetic** | DJ brand |
| **DO GOOD X** | AI accelerator |
| **NYC PIVOT** | Creative Coding with AI for NYC schools |

---

## HOW TO START THE NEXT SESSION

Say: *"Starting a new Super Connector session. Read HANDOFF_2026_04_01.md from the keyona-rerev/super-connector-api repo. SC_API_KEY is [key]. Today we're rebuilding the Contacts page into a proper CRM."*

Claude will read this file, load the super-connector-system skill, and know that layout is Option B and source/tags field is confirmed YES — no decisions to re-litigate.
