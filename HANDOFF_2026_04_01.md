# SUPER CONNECTOR SYSTEM — HANDOFF DOCUMENT
## Session: April 1, 2026
**Owner:** Keyona Meeks
**Status:** Events feature fully built — Railway schema, API endpoints, and web app UI all deployed
**Next session:** Initiative modal sub-project detail → episode guest slots + production status (Step 6 from build order)

---

## WHAT CHANGED THIS SESSION

### 1. Events Feature — Complete

**Option C hybrid model confirmed:**
- Interview series episodes remain as **sub-projects** under their initiative
- All other events (panels, workshops, networking) are a new **Events** first-class object

#### Railway — `db.py`
Two new tables added to `init_db()` via `CREATE TABLE IF NOT EXISTS` (auto-creates on next deploy, no migration needed):

**`events` table:**
```
event_id     TEXT PRIMARY KEY  (auto: EVT-{timestamp})
data         JSONB NOT NULL
created_at   TIMESTAMP
updated_at   TIMESTAMP
```
Indexes on `data->>'venture'` and `data->>'event_type'`.

JSONB data fields: `event_id` · `event_name` · `event_type` · `venture` · `date` · `location` · `status` · `initiative_id` · `description` · `notes`

**`event_guests` table:**
```
guest_id    TEXT PRIMARY KEY  (auto: EG-{timestamp})
event_id    TEXT NOT NULL
contact_id  TEXT
data        JSONB NOT NULL
created_at  TIMESTAMP
updated_at  TIMESTAMP
```
Indexes on `event_id` and `contact_id`.

JSONB data fields: `guest_id` · `event_id` · `contact_id` · `full_name` · `role` · `guest_status` · `notes`

New db functions: `upsert_event`, `get_event`, `get_all_events` (filterable by `event_type` and `venture`), `delete_event`, `upsert_event_guest`, `get_guests_for_event`, `delete_event_guest`

#### Railway — `models.py`
Four new Pydantic models: `EventPayload`, `EventStatusUpdate`, `EventGuestPayload`, `EventGuestUpdate`

**Status values:** `Planning` / `Confirmed` / `Complete` / `Cancelled`
**Event type values:** `Hosting` / `Attending` / `Workshop`
**Guest role values:** `Speaker` / `Panelist` / `Attendee` / `Interviewee` / `Organizer` / `Sponsor`
**Guest status values:** `Invited` / `Confirmed` / `Attended` / `No-show` / `Declined`

#### Railway — `main.py` — 10 new endpoints
```
GET    /events                    List all events (?type=Hosting&venture=BTC)
GET    /event/{id}                Fetch event + guests + guest_summary {total, confirmed, attended}
POST   /event                     Create event
PUT    /event/{id}                Update event (merge — omitted fields preserved)
DELETE /event/{id}                Remove event
PATCH  /event/{id}/status         Quick status update
GET    /event/{id}/guests         List guests for event
POST   /event-guest               Add guest to event
PATCH  /event-guest/{id}          Update guest role/status/notes (all optional, PATCH semantics)
DELETE /event-guest/{id}          Remove guest from event
```

`GET /event/{id}` returns event data + full guest list + `guest_summary: { total, confirmed, attended }` in a single call — web app uses this to render the badge without a second fetch.

All 10 endpoints confirmed live in Railway Swagger docs at `/docs`.

#### Web App — `index.html` (keyona-rerev/super-connector-app)
File grew from 81,982 → 98,309 bytes.

**New sidebar nav item:** `◈ Events` (below Content Registry)

**New Events page** (`page-events`):
- 3-tab switcher: Hosting · Attending · Workshops
- Active tab pre-selected when opening Create Event modal
- Event cards: name, date, venture, location, status badge with left-border color (Planning=blue, Confirmed=green, Complete=grey, Cancelled=red), guest count badge (`confirmed/total`) when guests exist

**New Event Detail Modal** (`.emodal-overlay`):
- Header: name, status badge, venture, date, location, connected initiative
- Section 1: About (description)
- Section 2: Guest Pipeline — inline status dropdowns per guest (updates via `PATCH /event-guest/{id}`), add-guest inline form (name + role + Add button → `POST /event-guest`)
- Section 3: Notes
- Quick actions: Mark Confirmed / Mark Complete (fires `PATCH /event/{id}/status`)

**New Create Event Modal** (`event-modal`):
- Fields: name, type (pre-filled from active tab), venture, date, status, location, description, notes

**New JS functions:**
`loadEvents()` · `renderEvents()` · `switchEventsTab()` · `openEventDetail()` · `addGuest()` · `updateGuestStatus()` · `quickStatusUpdate()` · `closeEModal()` · `handleEModalOverlayClick()` · `openEventModal()` · `closeEventModal()` · `saveEvent()`

**`showPage()` updated** to handle `'events'` — topbar button becomes `+ New Event`, calls `loadEvents()` on nav.

---

## PREVIOUS SESSION CHANGES (carried forward for context)

### Google Sheets Deleted
Both sheets deleted. Railway is sole source of truth.
- **Network CRM Sheet** `1WO6YK2alMx7Wu49Vpm1NZBN5fdquNUmCvgpIhjNK10g` — DELETED
- **Initiatives Sheet** `1Mvo3qP0KM1PgYl4rx8W9Dh2i78_9FmzHl5u-BHda8B0` — DELETED

### Phoebe GAS — Fully Rewired to Railway
**Script ID:** `1UeO72LgmCgEhr534Aw2ouj7lHKFiXe0mbGSCaFjsf1bphf510SOnkV4e`
All `SpreadsheetApp` calls removed. Railway-only via `Railway.gs`.

### Network CRM GAS — Fully Rewired to Railway
**Script ID:** `144ZiIEVpxYrRB93wq4MoYgtUxqVDQdQ7ZdmHKQH3RGWu-thi8F-6v7hz`
New `RailwayClient.gs`, all sheet reads replaced with Railway API calls.

### Web App — Sheet-Free (was already)
`super-connector-app` talks directly to Railway. No GAS, no sheet dependency.

---

## SYSTEM ARCHITECTURE (CURRENT STATE)

```
Gmail / Google Calendar
        ↕
GAS Scripts (two projects — time-triggered)
  • Phoebe: check-ins, reply parsing, decay scan, task sync
  • Network CRM: calendar briefings, email reply processing, Google Contacts import
        ↕
Railway API (FastAPI + pgvector) ← SOLE SOURCE OF TRUTH
  https://super-connector-api-production.up.railway.app
        ↕
GitHub Pages Web App ← PRIMARY UI
  https://keyona-rerev.github.io/super-connector-app
```

---

## RAILWAY DATABASE STATE

| Table | Records | Notes |
|---|---|---|
| `contacts` | 4,401 | Vectorized (512-dim Voyage AI), health scored |
| `initiatives` | 26 | All migrated |
| `sub_projects` | 0 | Empty, ready |
| `stakeholders` | 9 | Sparse — populate via web app |
| `activation_angles` | 0 | Empty, ready |
| `action_items` | 0 | Empty, ready |
| `follow_ups` | 0 | Empty, ready |
| `events` | 0 | ✅ Table built this session — ready |
| `event_guests` | 0 | ✅ Table built this session — ready |

---

## KEY IDs & CREDENTIALS

| Resource | ID / URL |
|---|---|
| Railway API | `https://super-connector-api-production.up.railway.app` |
| Web App | `https://keyona-rerev.github.io/super-connector-app` |
| GitHub Org | `keyona-rerev` |
| API Repo | `keyona-rerev/super-connector-api` |
| App Repo | `keyona-rerev/super-connector-app` |
| Phoebe GAS Script | `1UeO72LgmCgEhr534Aw2ouj7lHKFiXe0mbGSCaFjsf1bphf510SOnkV4e` |
| Network CRM GAS Script | `144ZiIEVpxYrRB93wq4MoYgtUxqVDQdQ7ZdmHKQH3RGWu-thi8F-6v7hz` |
| Tool Registry Sheet | `1ol8Yvpe454T4yHpEgy8PFjOAJvfyUDrKesgD_wM7m4o` |
| Prismm Martha ABM Sheet | `1FN4NVRb7VfbUg7EO6G01cYm3p9-Q2jS1R9gNl_BU410` |
| Transcript Folder | `1kU30t4XelF4f3HDi7h0dbrq5YIPf15tg` |

**SC_API_KEY:** Ask Keyona at session start. Never hardcode.

---

## OPEN KNOWN ISSUES (CARRIED FORWARD)

1. **Stakeholders table sparse (9 records).** Calendar briefings only fire for those 9. Populate via web app as initiatives get stakeholders.
2. **Interactions not in Railway schema.** `createInteraction` / `getInteractions` in Network CRM GAS are stubbed.
3. **Content tab → Railway sync not built.** `POST /content` endpoint exists. GAS trigger not yet wired.
4. **Follow-up health scoring not built.** Overdue follow-ups should degrade Relationship Health on contact. Phase 2.
5. **Apollo enrichment pending.** 406 credits remaining. Needs `APOLLO_API_KEY` in Network CRM GAS Script Properties, then run `enrichWithApollo()`. Re-vectorize after.
6. **Google Contacts import.** `importGoogleContacts()` in GoogleSync.gs ready to run. Trigger installs Sunday 9am via `installGoogleSyncTriggers()`.

---

## NEXT SESSION: INITIATIVE MODAL — EPISODE DETAIL (Step 6)

The Events feature is complete. The remaining item from the original build order is:

**Step 6 — Initiative modal sub-project detail panel: episode guest slots + production status**

When a sub-project inside the initiative modal is clicked, the module detail panel should show:
- Guest slots (each slot = a contact/stakeholder with their interview status)
- Production status pipeline: Pitching → Scheduled → Recorded → Editing → Published
- Content asset output link

This applies specifically to the BTC Climate Tech Exit Lab interview series sub-projects (and any future interview-format sub-projects). The sub-project data model already supports this via `notes` and `status` fields — it may need a light schema extension or a convention for storing guest slot data in JSONB.

**Suggested approach for next session:**
1. Decide: extend `sub_projects` JSONB to include `episode_guests: []` and `production_status` fields, or model episode guests as event-guests via an EVT record per episode.
2. Update `PUT /sub-project/{id}` or create a new `/episode-guest` endpoint if needed.
3. Build the episode detail UI panel inside the existing initiative modal.

---

## VENTURES REFERENCE

| Venture | Focus |
|---|---|
| **ReRev Labs** | AI education, consulting, automation sales |
| **Prismm** | White-label digital vault, B2B to community banks/credit unions. Co-founder: Martha |
| **Black Tech Capital (BTC)** | Climate tech nano VC. Key initiative: Climate Tech Exit Lab interview series |
| **Sekhmetic** | DJ brand |
| **DO GOOD X** | AI accelerator |
| **NYC PIVOT** | Creative Coding with AI for NYC schools |

---

## HOW TO START THE NEXT SESSION

Say: *"Starting a new Super Connector session. Read HANDOFF_2026_04_01.md from the keyona-rerev/super-connector-api repo. SC_API_KEY is [key]. Today we're building the episode detail panel inside the initiative modal."*

Claude will read this file, load the super-connector-system skill, and be fully oriented.
